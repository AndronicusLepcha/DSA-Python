#include <stdio.h>

int main() {
    int myArray[5] = {1, 2, 3, 4, 5};

    // Printing the memory address (index) of the array
    printf("Memory Address of myArray: %p\n", (void*)myArray);
    

    return 0;
}